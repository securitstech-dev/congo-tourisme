{
  "name": "congo-tourisme",
  "private": true,
  "packageManager": "npm@11.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "vercel-build": "cd apps/frontend && npm run build"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "turbo": "latest"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}
